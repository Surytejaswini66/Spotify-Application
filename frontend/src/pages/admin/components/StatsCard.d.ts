type StatsCardProps = {
    icon: React.ElementType;
    label: string;
    value: string;
    bgColor: string;
    iconColor: string;
};
declare const StatsCard: ({ bgColor, icon: Icon, iconColor, label, value }: StatsCardProps) => import("react/jsx-runtime").JSX.Element;
export default StatsCard;
